<%= form_with(model: avaliacao) do |form| %>
  <% if avaliacao.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(avaliacao.errors.count, "error") %> prohibited this avaliacao from being saved:</h2>

      <ul>
        <% avaliacao.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :pontos, style: "display: block" %>
    <%= form.text_field :pontos %>
  </div>

  <div>
    <%= form.label :observacoes, style: "display: block" %>
    <%= form.text_field :observacoes %>
  </div>

  <div>
    <%= form.label :atividade_id, style: "display: block" %>
    <%= collection_select(:avaliacao, :atividade_id, Atividade.all, :id, :atividade_completa) %>
  </div>

  <div>
    <%= form.label :aluno_id, style: "display: block" %>
    <%= collection_select(:avaliacao, :aluno_id, Aluno.all, :id, :nome) %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
